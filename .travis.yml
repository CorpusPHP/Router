language: php
dist: precise

php:
  - 7.1
  - 7.2
  - hhvm

matrix:
  allow_failures:
    - php: nightly
    - php: hhvm

install: composer install
script: vendor/bin/phpunit --coverage-clover=coverage.clover
